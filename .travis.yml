os: linux
dist: bionic
services:
  - xvfb
install:
  - sudo snap install snapcraft --classic
  - sudo snap install multipass --classic
  - git clone --depth 3 --single-branch --branch develop https://github.com/cyberbotics/webots.git
script:
  - snapcraft
